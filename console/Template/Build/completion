# Winter Framework - Shell Tab Completion
# Generated by: call cfg completion
# Install:      call cfg completion -i
# Manual:       source this file from your shell config

# === BASH: source from ~/.bashrc ===
# _php_winter_call_find() {
#     local dir="$PWD"
#     while [ "$dir" != "/" ]; do
#         [ -f "$dir/call" ] && { echo "$dir/call"; return 0; }
#         dir="$(dirname "$dir")"
#     done
#     return 1
# }
# _php_winter_call() {
#     local script completions
#     script=$(_php_winter_call_find) || return
#     local IFS=$'\n'
#     completions=$(COMP_LINE="$COMP_LINE" COMP_POINT="$COMP_POINT" \
#         php "$script" complete 2>/dev/null)
#     COMPREPLY=($(compgen -W "$completions" -- "${COMP_WORDS[$COMP_CWORD]}"))
#     return 0
# }
# complete -o default -F _php_winter_call call

# === ZSH: place in ~/.zsh/completions/_call (fpath) ===
# #compdef call
# _php_winter_call_find() {
#     local dir="$PWD"
#     while [[ "$dir" != "/" ]]; do
#         [[ -f "$dir/call" ]] && { echo "$dir/call"; return 0; }
#         dir="${dir:h}"
#     done
#     return 1
# }
# _php_winter_call() {
#     local script completions
#     script=$(_php_winter_call_find) || return
#     local -a items
#     completions=$(COMP_LINE="$BUFFER" COMP_POINT="$CURSOR" \
#         php "$script" complete 2>/dev/null)
#     items=(${(f)completions})
#     _describe 'call' items
# }
# _php_winter_call "$@"
