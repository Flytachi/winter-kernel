<?php

namespace __namespace__;

use Flytachi\Winter\Redis\Store\RedisStore;

class __className__ extends RedisStore
{
    /** The endpoint this store talks to. */
    protected string $redisConfigClassName = MainRedisConfig::class;

    /** Prepended to every key, so several stores share one database without colliding. */
    protected string $prefix = '__prefix__';
}
